import { terser } from "rollup-plugin-terser";

export default {
  input: 'src/flatout.js',
  plugins: [terser()],
  output: {
    format: 'esm',
    name: 'flatout',
    file: 'dist/flatout.min.js'
  }
}
